# 5th stage (s): post installation
depends:
  - asterisk.caret
  - nixos.o

actions:
  # Keep 7 generations:
  - action: command.run
    command: nix-env
    args: [--delete-generations, "+7"]
  - action: command.run
    command: nix-store
    args: [--gc]
