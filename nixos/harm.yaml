depends:
  - nixos

# Hyper-V in ARM64
actions:
  - action: file.copy
    from: kernel-harm.nix
    owned_by_group: root
    owned_by_user: root
    template: true
    to: '{{ variables.root }}/etc/nixos/kernel-harm.nix'
